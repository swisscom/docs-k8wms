openapi: 3.0.0
info:
  version: 0.1.0
  title: Schema for data values, generated by ytt
  description: This schema is used to customize the deployment of k8wms_external_secrets
servers:
- url: https://www.gitlab.com/api/
security:
  - ApiKeyAuth: []
tags:
- name: datavalues
  description: Operations about datavalues
paths:
  /api/v4/groups/tool:
    get:
      tags:
      - datavalues
      summary: Gets datavalues of this tool.
      description: Datavalues of external secrets
      responses:
        200:
          description: Gets tool datavalues
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dataValues'

components:
  schemas:
    dataValues:
      type: object
      additionalProperties: false
      properties:
        tool:
          title: k8wms_external_secrets
          type: object
          additionalProperties: false
          description: This schema is used to customize the deployment of k8wms_external_secrets
          properties:
            name:
              type: string
              default: k8wms_external_secrets
            version:
              type: string
              default: 0.9.9-1
            helm:
              type: object
              additionalProperties: false
              properties:
                repository:
                  type: string
                  default: ""
            container:
              type: object
              additionalProperties: false
              properties:
                registry:
                  type: string
                  default: ""
            ha:
              type: object
              additionalProperties: false
              properties:
                enabled:
                  type: string
                  default: ""
            imagePullSecretKey:
              type: string
              default: ""
            azureKeyVault:
              title: Azure Key vault configuration
              type: object
              additionalProperties: false
              description: if enabled external secrets will create an ClusterSecretStore to access this keyvault
              x-example-description: Example for azureKeyVault
              example:
                enabled: true
                vaultURL: https://k8wms.vault.azure.net
                clientID: <ID of the mananaged Identy that is used to access keyvault>
                tenantID: <Entra ID tenantID>
              properties:
                enabled:
                  type: boolean
                  default: false
                vaultURL:
                  type: string
                  default: ""
                clientID:
                  type: string
                  default: ""
                tenantID:
                  type: string
                  default: ""
